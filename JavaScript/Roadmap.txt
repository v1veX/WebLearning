Базовый синтаксис

подключение к странице, панель консоли в дэв тулз
типы данных, проверка типов и преобразование типов
арифметические и логические операторы
условия, тернарный оператор
циклы
console.log
объекты(методы и свойства, this, опциональные цепочки,
	деструктуризация, оператор расширения)
методы чисел
методы строк
методы массивов
перебор объекта, преобразование объекта в массив
Set, Map, WeakSet, WeakMap
JSON формат, парсинг и преобразование данных
функции (hoisting, this, context, лексическая область видимости, замыкание)
setTimeout, setInterval
классы (объявление, наследование, конструктор, экземпляр, геттеры и сеттеры,
	приватные и защищенные методы, статические свойства и методы)
обработка ошибок (try-catch, класс Error)
асинхронность, async/await, промисы (EventLoop)
Модули, import и export

В браузере

BOM и DOM
поиск элементов в DOM
получение значений свойст и атрибутов у элемента
добавление, удаление и изменение элементов в DOM-дереве
события в браузере: click, input, keyup, scroll, resize и т.д.
добавление и удаление событий к элементам
всплытие и погружение событий
генерация пользовательских событий
FormData API
fetch
хранение данных в браузере (куки, локальное хранилище,
	сессионное хранилище, IndexDB)
History API
